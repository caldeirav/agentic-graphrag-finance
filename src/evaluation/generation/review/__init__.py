"""Human-in-the-loop dataset quality review for custom-judge bundles (018).

This package extends judge-generation (011/017) with review queues, annotations,
and override application. It MUST NOT import retrieval or ingestion paths.
"""
