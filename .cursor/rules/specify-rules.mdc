---
alwaysApply: true
---

<!-- SPECKIT START -->
For additional context about technologies, project structure, shell commands, and
pipeline segments, read the current implementation plan:

**Plan**: `specs/012-research-repro-kit/plan.md` (active) | Prior: `specs/011-judge-eval-dataset/plan.md` | Base: `specs/001-sec-disclosure-rag/plan.md`

Quick reference:
- **Stack**: uv, Docling/XBRL graph corpus (LFS), LangGraph agent, sentence-transformers (flat-chunk), MLflow, Gemini judge, LM Studio LLM
- **Layers**: `src/evaluation/reproduction/` (repro kit), `src/evaluation/` (runner + custom-judge), `src/retrieval/` (ablation flags), `src/cli/commands/repro.py`
- **CLI**: `agent-query repro run-all --manifest releases/paper-v1.0/manifest.yaml` | dataset: custom-judge dev split only for headline tables
- **Storage**: Release manifests `releases/{tag}/`; bundles `data/benchmarks/custom-judge/v{version}/`; outputs `reports/repro-{tag}/tables/`
- **Variants (paper-v1.0)**: graph-full, flat-chunk (dense embedding), ablation-no-macro, ablation-no-walker, ablation-xbrl-only
- **Phases**: A manifest/verify → B relevance labels → C variant backends → D export/verify-tables → E paper tag + CI smoke
- **Design artifacts**: `specs/012-research-repro-kit/{research,data-model,quickstart}.md` and `contracts/`
<!-- SPECKIT END -->
