---
alwaysApply: true
---

<!-- SPECKIT START -->
For additional context about technologies, project structure, shell commands, and
pipeline segments, read the current implementation plan:

**Plan**: `specs/007-ask-console-trace/plan.md` (active) | Prior: `specs/005-html-narrative-supplement/plan.md` | Base: `specs/001-sec-disclosure-rag/plan.md`

Quick reference:
- **Stack**: uv, EDGAR ingest (`SEC_EDGAR_USER_AGENT`), Docling, docling-graph, LangGraph (+ intent router), Qwen/LM Studio, MLflow, Gemini judge, **Rich console trace**
- **Layers**: `src/ingestion/`, `src/parsing/`, `src/graph/`, `src/retrieval/`, `src/tracing/console_trace/`, `src/evaluation/`, `src/cli/`
- **CLI**: `uv run agent-query ask --ticker AAPL --trace normal --query "..."` (trace on stderr; answer on stdout)
- **Storage**: `data/raw/sec_downloads/`, `data/parsed/`, `data/graphs/{issuer}/`
- **Phases**: P1 trace models/registry → P2 node wrappers + LLM trace → P3 JSONL + contract tests
- **Design artifacts**: `specs/007-ask-console-trace/{research,data-model,quickstart}.md` and `contracts/`
<!-- SPECKIT END -->
