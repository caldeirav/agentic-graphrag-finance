---
alwaysApply: true
---

<!-- SPECKIT START -->
For additional context about technologies, project structure, shell commands, and
pipeline segments, read the current implementation plan:

**Plan**: `specs/014-repro-results-viewer/plan.md` (active) | Prior: `specs/013-benchmark-eval-acceleration/plan.md` | Base: `specs/001-sec-disclosure-rag/plan.md`

Quick reference:
- **Stack**: uv, custom-judge repro artifacts, static HTML report generation, LaTeX/CSV/Markdown table copy, optional MLflow deep links
- **Layers**: `src/evaluation/reproduction/` (report loader/render), `src/cli/commands/repro.py` (report command)
- **CLI**: `agent-query repro report --input reports/repro-{tag}` | `repro report --format latex-only --table headline`
- **Storage**: input `reports/repro-{tag}/repro_run.json`, `{variant}/results.json`, `tables/*.csv`; output `report.html`
- **Focus**: read-only run investigation + paper-ready table copy; no re-run of judge/agent/eval
- **Design artifacts**: `specs/014-repro-results-viewer/{research,data-model,quickstart}.md` and `contracts/`
<!-- SPECKIT END -->

