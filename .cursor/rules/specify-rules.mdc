---
alwaysApply: true
---

<!-- SPECKIT START -->
For additional context about technologies, project structure, shell commands, and
pipeline segments, read the current implementation plan:

**Plan**: `specs/013-benchmark-eval-acceleration/plan.md` (active) | Prior: `specs/012-research-repro-kit/plan.md` | Base: `specs/001-sec-disclosure-rag/plan.md`

Quick reference:
- **Stack**: uv, custom-judge LFS corpus, LangGraph agent, sentence-transformers (flat-chunk), MLflow, Gemini judge (batched when deferred), LM Studio LLM
- **Layers**: `src/evaluation/reproduction/` (defer judge, item subgraph, resume), `src/retrieval/service.py` (defer guard), `src/cli/commands/repro.py`
- **CLI**: `agent-query repro run-all --defer-judge --resume` | `repro judge-batch` | `repro export-tables`
- **Storage**: `reports/repro-{tag}/repro_run.json`, `{variant}/results.json`, `tables/`
- **Flags**: `REPRO_DEFER_JUDGE`, `REPRO_JUDGE_CONCURRENCY`; per-item graph slice from `expected_bindings`
- **Phases**: A defer judge + judge-batch → B per-item subgraph → C variant resume + recovery docs
- **Design artifacts**: `specs/013-benchmark-eval-acceleration/{research,data-model,quickstart}.md` and `contracts/`
<!-- SPECKIT END -->
