---
alwaysApply: true
---

<!-- SPECKIT START -->
For additional context about technologies, project structure, shell commands, and
pipeline segments, read the current implementation plan:

**Plan**: `specs/017-custom-judge-v2/plan.md` (active) | Prior: `specs/016-fair-outcome-scoring/plan.md` | Base: `specs/001-sec-disclosure-rag/plan.md`

Quick reference:
- **Stack**: uv, custom-judge bundle v2.0.0 (net-new), judge v3.1 VA-only task_success, paper-v2.0 release lock, static HTML report
- **Layers**: `src/evaluation/generation/` (v2 gates, macro feasibility, publish audit), `src/evaluation/reproduction/` (export/report v2 semantics), `src/retrieval/macro/validator.py` (bindability gate)
- **CLI**: `benchmark-dataset generate --config custom_judge_v2.yaml` → audit 20 items → `publish --version 2.0.0`; `repro run --release paper-v2.0` → judge-batch → export-tables → report
- **Storage**: bundle `data/benchmarks/custom-judge/v2.0.0/`; release `releases/paper-v2.0/manifest.yaml`; repro `reports/repro-paper-v2.0/`
- **Focus**: 100% answer-GT, comparison_structured answers, ≥40 multi-filing items, macro gate all 200, task_success n=200 VA-only, no rubric_alignment in v2 reports
- **Design artifacts**: `specs/017-custom-judge-v2/{research,data-model,quickstart}.md` and `contracts/`
<!-- SPECKIT END -->
