---
alwaysApply: true
---

<!-- SPECKIT START -->
For additional context about technologies, project structure, shell commands, and
pipeline segments, read the current implementation plan:

**Plan**: `specs/015-repro-eval-validity/plan.md` (active) | Prior: `specs/014-repro-results-viewer/plan.md` | Base: `specs/001-sec-disclosure-rag/plan.md`

Quick reference:
- **Stack**: uv, custom-judge repro artifacts, deferred judge re-score, structural audit metrics, stratified export tables, static HTML report (014)
- **Layers**: `src/evaluation/reproduction/` (export, judge_batch, structural, stratum, report), `src/evaluation/judges/outcome_scoring.py`, `src/cli/commands/repro.py`
- **CLI**: `repro judge-batch` → `export-tables` → `report` on `reports/repro-{tag}`; `--force-rescore` for full re-judge
- **Storage**: input checkpoints + bundle; output `tables/by_evidence_source.csv`, `variant_delta_by_source.csv`, enriched `repro_run.json`, `report.html`
- **Focus**: trustworthy outcome metrics after P0, structural audit wiring, aggregated investigation notes, HTML/XBRL/mixed stratum ablations
- **Design artifacts**: `specs/015-repro-eval-validity/{research,data-model,quickstart}.md` and `contracts/`
<!-- SPECKIT END -->

