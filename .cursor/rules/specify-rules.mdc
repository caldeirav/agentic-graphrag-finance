---
alwaysApply: true
---

<!-- SPECKIT START -->
For additional context about technologies, project structure, shell commands, and
pipeline segments, read the current implementation plan:

**Plan**: `specs/012-judge-eval-dataset/plan.md` (active) | Prior: `specs/010-mlflow-trajectory-judge-eval/plan.md` | Base: `specs/001-sec-disclosure-rag/plan.md`

Quick reference:
- **Stack**: uv, EDGAR ingest, Docling/XBRL, docling-graph, LangGraph, MLflow Traces, Gemini 2.5 Pro (generation + judge), Git LFS corpus bundles
- **Layers**: `src/ingestion/`, `src/parsing/`, `src/graph/`, `src/retrieval/`, `src/tracing/`, `src/evaluation/generation/` (dataset builder), `src/evaluation/` (registry + judge), `src/cli/`
- **CLI**: `agent-query benchmark-dataset generate|publish|reproduce|extend` | eval: `custom-judge` registry dataset, offline via bundled snapshot
- **Storage**: Published bundles `data/benchmarks/custom-judge/v{version}/`; LFS under `corpus/`; allowlist `configs/benchmarks/issuer_allowlist_v1.json`
- **Configs**: `configs/benchmarks/custom_judge_v1.yaml`, inspiration profiles under `configs/benchmarks/inspiration_profiles/`
- **Phases**: A scaffold → B sample+materialize → C judge generate → D bundle CLI → E registry+offline eval → F publish v1 (≥200 items)
- **Design artifacts**: `specs/012-judge-eval-dataset/{research,data-model,quickstart}.md` and `contracts/`
<!-- SPECKIT END -->
